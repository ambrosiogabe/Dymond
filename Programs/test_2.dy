class Circle {
  func init(radius) {
    this.radius = radius;
  }

  func area() {
    return 3.14 * this.radius * this.radius;
  }

  static square(n) {
    return n * n;
  }
}


func hello(name) {
  print("Hello, " + name);
}

var circle = Circle(2);
circle.hello = hello("bob");
